<% content_for :page_title do %>
  <%= link_to(Spree.t(:analytics), admin_analytics_path) %> / <%= Spree.t(:products) %>
<% end %>

<%= render 'spree/admin/shared/analytics_vue_mount' do %>
  <gapi-auth
    ga-token="<%= @ga_token %>"
    ga-view-id="<%= @ga_view_id %>"
    v-slot="{ gapi, authorized, gaViewId }">
    <reports-with-date v-slot="{ startDate, endDate }">
      <products-report :gapi="gapi"
                       :authorized="authorized"
                       :ga-view-id="gaViewId"
                       :start-date="startDate"
                       :end-date="endDate">
      </products-report>
    </reports-with-date>
  </gapi-auth>
<% end %>
